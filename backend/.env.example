PORT=4000

# ─── Base de datos ───────────────────────────────────────────
# Opción A (default): Neon cloud — descomenta esta línea y comenta las de abajo
# DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Opción B: PostgreSQL local — comenta DATABASE_URL y descomenta estas:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=kurzoz
